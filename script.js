(() => {
  const i18n = {
    it: {
      brand: "I tuoi documenti restano tuoi.",
      navPricing: "Investimento",
      navComparison: "Confronto",
      navFeatures: "Vantaggi",
      navSoftware: "Software",
      navTry: "Richiedi demo",
      navBuy: "Acquista",
      beforeLabel: "Prima",
      beforeText:
        "Sostituisci 30.000 parole a mano oppure copia-incolli da ChatGPT in cloud.",
      afterLabel: "Dopo",
      afterText:
        'Traduzione in 30min e controlla.<br>Quanto è accurato? <a href="__EXAMPLE_URL__">Vedi esempio</a> o <a href="__TRY_URL__" data-cta="try">Richiedi demo</a>.',
      heroNote:
        "Comparazione basata su un documento medio di 150 pagine con paragrafi, tabelle, elenchi, riferimenti. Il testo all’interno delle immagini <strong>non</strong> viene tradotto.",
      pdfCompareAria: "Confronto documenti",
      pdfCompareLabel: "Confronto",
      pdfCompareTitle: "Originale e traduzione, affiancati.",
      pdfCompareSubtitle:
        "Tutte le caselle di testo all'interno del documento vengono automaticamente estratte e tradotte tenendo conto del contesto circostante. Le immagini vengono lasciate come sono, anche se le immagini contengono scritte, non vengono modificate.",
      pdfCompareEn: "Inglese",
      pdfCompareIt: "Italiano",
      pdfCompareLoading: "Caricamento documenti…",
      ctaHero: "Richiedi demo",
      featuresAria: "Vantaggi",
      featPrivacyTitle: "100% privato",
      featPrivacyText:
        "Tutti i tuoi documenti restano sul tuo MiniPc 128GB. Nessun dato esce mai.",
      featSpeedTitle: "30–60 minuti",
      featSpeedText:
        "Invece che settimane in uno studio esterno. Un intero manuale in meno di un’ora.",
      featDocxTitle: "Mantiene il docx",
      featDocxText:
        "Elenchi, tabelle, formattazioni, intestazioni, note e immagini restano invariate.",
      featLdapTitle: "Auth LDAP",
      featLdapText:
        "Accesso aziendale con LDAP. Controlli chi entra, senza account esterni.",
      galleryAria: "Galleria",
      imgLaunch: "MiniPc 128GB con flusso di traduzione documenti",
      imgSystem: "Sistema di traduzione su MiniPc 128GB",
      imgProduct: "Lancio prodotto Translation System on MiniPc 128GB",
      videoHint: "Video in arrivo",
      softwareLabel: "Document-first",
      softwareTitle: "Carica. Traduci. Controlla. Esporta.",
      imgScreenshot: "Interfaccia di traduzione",
      pricingLabel: "Investimento",
      pricingProduct: "MiniPc 128GB + Traduzioni",
      pricingDesc: "Hardware + sistema di traduzione. Locale. Privato. Pronto all’uso.",
      pricingCta: "Paga ora",
      pricingBoxAria: "Investimento e risparmio",
      roiPrefix: "da",
      roiSuffix: "in media",
      roiPages30: "30 pagine",
      roiPages50: "50 pagine",
      roiPages80: "80 pagine",
      roiPages120: "120 pagine",
      roiPages200: "200 pagine",
      roiPayback: "Ritorno sull'investimento in",
      roiDocsUnit: "documenti",
      roiAssumptionsLabel: "Assunzioni",
      roiAssumptions:
        "Documento di riferimento 100 pagine = € 2.000; 250 parole/pagina; tariffa € 0,08/parola (equivalente a € 20/ora a 250 parole/ora); 70% di tempo risparmiato.",
      footer:
        "Sviluppato da <strong>Luca Miglioli</strong> — AI Engineer &amp; Full-Stack Developer",
    },
    en: {
      brand: "Your documents stay yours.",
      navPricing: "Investment",
      navComparison: "Comparison",
      navFeatures: "Features",
      navSoftware: "Software",
      navTry: "Request demo",
      navBuy: "Buy",
      beforeLabel: "Before",
      beforeText:
        "Replace 30,000 words by hand or copy-paste from ChatGPT in the cloud.",
      afterLabel: "After",
      afterText:
        'Translation in 30min and review.<br>How accurate is it? <a href="__EXAMPLE_URL__">See example</a> or <a href="__TRY_URL__" data-cta="try">Request demo</a>.',
      heroNote:
        "Comparison based on a typical 150-page document with paragraphs, tables, lists, and references. Text inside images is <strong>not</strong> translated.",
      pdfCompareAria: "Document comparison",
      pdfCompareLabel: "Comparison",
      pdfCompareTitle: "Original and translation, side by side.",
      pdfCompareSubtitle:
        "All text boxes in the document are automatically extracted and translated with surrounding context in mind. Images are left unchanged — even when they contain text, they are not modified.",
      pdfCompareEn: "English",
      pdfCompareIt: "Italian",
      pdfCompareLoading: "Loading documents…",
      ctaHero: "Request demo",
      featuresAria: "Features",
      featPrivacyTitle: "100% private",
      featPrivacyText:
        "All your documents stay on your MiniPc 128GB. No data ever leaves.",
      featSpeedTitle: "30–60 minutes",
      featSpeedText:
        "Instead of weeks at an external agency. A full manual in under an hour.",
      featDocxTitle: "Keeps the docx",
      featDocxText:
        "Lists, tables, formatting, headings, notes, and images stay intact.",
      featLdapTitle: "LDAP auth",
      featLdapText:
        "Enterprise access with LDAP. You control who gets in — no external accounts.",
      galleryAria: "Gallery",
      imgLaunch: "MiniPc 128GB with document translation flow",
      imgSystem: "Translation system on MiniPc 128GB",
      imgProduct: "Product launch Translation System on MiniPc 128GB",
      videoHint: "Video coming soon",
      softwareLabel: "Document-first",
      softwareTitle: "Upload. Translate. Review. Export.",
      imgScreenshot: "Translation interface",
      pricingLabel: "Investment",
      pricingProduct: "MiniPc 128GB + Translations",
      pricingDesc: "Hardware + translation system. Local. Private. Ready to use.",
      pricingCta: "Pay now",
      pricingBoxAria: "Investment and savings",
      roiPrefix: "from",
      roiSuffix: "on average",
      roiPages30: "30 pages",
      roiPages50: "50 pages",
      roiPages80: "80 pages",
      roiPages120: "120 pages",
      roiPages200: "200 pages",
      roiPayback: "Return on investment in",
      roiDocsUnit: "documents",
      roiAssumptionsLabel: "Assumptions",
      roiAssumptions:
        "Reference document 100 pages = €2,000; 250 words/page; rate €0.08/word (equivalent to €20/hour at 250 words/hour); 70% time saved.",
      footer:
        "Developed by <strong>Luca Miglioli</strong> — AI Engineer &amp; Full-Stack Developer",
    },
  };

  const DEMO_URL =
    "https://www.linkedin.com/feed/update/urn:li:share:7490408785249902592/";

  const buildTryUrl = () => DEMO_URL;

  const applyTryLinks = () => {
    const href = buildTryUrl();
    document.querySelectorAll('[data-cta="try"]').forEach((el) => {
      el.href = href;
      el.target = "_blank";
      el.rel = "noopener noreferrer";
    });
  };

  const INVESTMENT = 9600;
  const REF_PAGES = 100;
  const REF_COST = 2000;
  const COST_PER_PAGE = REF_COST / REF_PAGES;
  const TIME_SAVED = 0.7;
  const ROI_DEFAULT = { pages: 120 };

  let currentLang = "it";
  let roiSelection = { ...ROI_DEFAULT };

  const formatRoiDocs = (docs) =>
    docs.toFixed(1);

  const calcRoiDocs = (pagesPerDoc) =>
    INVESTMENT / (pagesPerDoc * COST_PER_PAGE * TIME_SAVED);

  const updateRoiDisplay = () => {
    const docsEl = document.getElementById("roi-docs");
    if (!docsEl) return;

    const docs = calcRoiDocs(roiSelection.pages);
    docsEl.textContent = formatRoiDocs(docs);

    document.querySelectorAll(".roi-dropdown").forEach((dropdown) => {
      const key = dropdown.dataset.roi;
      const value = roiSelection[key];
      const label = dropdown.querySelector(".roi-dropdown__trigger span:first-child");
      const menu = dropdown.querySelector(".roi-dropdown__menu");
      if (!menu || value == null) return;

      menu.querySelectorAll("[role='option']").forEach((opt) => {
        const selected = Number(opt.dataset.value) === value;
        opt.setAttribute("aria-selected", selected ? "true" : "false");
        if (selected && label) label.textContent = opt.textContent;
      });
    });
  };

  const initRoi = () => {
    const dropdowns = [...document.querySelectorAll(".roi-dropdown")];
    if (!dropdowns.length) return;

    const closeAll = (except) => {
      dropdowns.forEach((dropdown) => {
        if (dropdown === except) return;
        dropdown.classList.remove("is-open");
        const trigger = dropdown.querySelector(".roi-dropdown__trigger");
        if (trigger) trigger.setAttribute("aria-expanded", "false");
      });
    };

    const setOpen = (dropdown, open) => {
      if (open) closeAll(dropdown);
      dropdown.classList.toggle("is-open", open);
      const trigger = dropdown.querySelector(".roi-dropdown__trigger");
      if (trigger) trigger.setAttribute("aria-expanded", open ? "true" : "false");
    };

    dropdowns.forEach((dropdown) => {
      const key = dropdown.dataset.roi;
      const trigger = dropdown.querySelector(".roi-dropdown__trigger");
      const menu = dropdown.querySelector(".roi-dropdown__menu");
      if (!key || !trigger || !menu) return;

      let closeTimer = null;

      const cancelClose = () => {
        if (closeTimer) {
          clearTimeout(closeTimer);
          closeTimer = null;
        }
      };

      const scheduleClose = () => {
        cancelClose();
        closeTimer = setTimeout(() => setOpen(dropdown, false), 220);
      };

      [dropdown, menu].forEach((el) => {
        el.addEventListener("mouseenter", () => {
          cancelClose();
          setOpen(dropdown, true);
        });
        el.addEventListener("mouseleave", scheduleClose);
      });

      trigger.addEventListener("click", (e) => {
        e.stopPropagation();
        cancelClose();
        setOpen(dropdown, true);
      });

      const applyOption = (opt) => {
        if (!opt?.dataset?.value) return;
        const next = Number(opt.dataset.value);
        if (roiSelection[key] === next) {
          updateRoiDisplay();
          return;
        }
        roiSelection = {
          ...roiSelection,
          [key]: next,
        };
        updateRoiDisplay();
      };

      menu.addEventListener("pointerover", (e) => {
        const opt = e.target.closest("[role='option']");
        if (!opt || !menu.contains(opt)) return;
        applyOption(opt);
      });

      menu.addEventListener("click", (e) => {
        const opt = e.target.closest("[role='option']");
        if (!opt) return;
        e.preventDefault();
        e.stopPropagation();
        applyOption(opt);
        setOpen(dropdown, false);
      });
    });

    document.addEventListener("click", (e) => {
      if (!dropdowns.some((d) => d.contains(e.target))) closeAll();
    });

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") closeAll();
    });

    updateRoiDisplay();
  };

  const setLang = (lang) => {
    const dict = i18n[lang];
    if (!dict) return;

    currentLang = lang;

    document.documentElement.lang = lang;
    localStorage.setItem("lang", lang);

    const tryHref = buildTryUrl();

    document.querySelectorAll("[data-i18n]").forEach((el) => {
      const key = el.getAttribute("data-i18n");
      if (dict[key] != null) el.textContent = dict[key];
    });

    document.querySelectorAll("[data-i18n-html]").forEach((el) => {
      const key = el.getAttribute("data-i18n-html");
      if (dict[key] != null) {
        el.innerHTML = dict[key]
          .replace(/__TRY_URL__/g, tryHref)
          .replace(/__EXAMPLE_URL__/g, "#comparison");
      }
    });

    document.querySelectorAll("[data-i18n-aria]").forEach((el) => {
      const key = el.getAttribute("data-i18n-aria");
      if (dict[key] != null) el.setAttribute("aria-label", dict[key]);
    });

    document.querySelectorAll("[data-i18n-alt]").forEach((el) => {
      const key = el.getAttribute("data-i18n-alt");
      if (dict[key] != null) el.setAttribute("alt", dict[key]);
    });

    applyTryLinks();
    updateRoiDisplay();

    document.querySelectorAll(".lang-btn").forEach((btn) => {
      const active = btn.dataset.lang === lang;
      btn.classList.toggle("is-active", active);
      btn.setAttribute("aria-pressed", active ? "true" : "false");
    });
  };

  document.querySelectorAll(".lang-btn").forEach((btn) => {
    btn.addEventListener("click", () => setLang(btn.dataset.lang));
  });

  initRoi();

  const saved = localStorage.getItem("lang");
  setLang(saved === "en" ? "en" : "it");

  const reveals = document.querySelectorAll(".reveal");

  if ("IntersectionObserver" in window) {
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("is-in");
            io.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.12, rootMargin: "0px 0px -6% 0px" }
    );
    reveals.forEach((el) => io.observe(el));
  } else {
    reveals.forEach((el) => el.classList.add("is-in"));
  }

  const video = document.getElementById("demo-video");
  if (video) {
    const markReady = () => {
      if (video.readyState >= 2) video.dataset.ready = "";
    };
    video.addEventListener("loadeddata", markReady);
    video.addEventListener("canplay", markReady);
    markReady();
  }
})();
